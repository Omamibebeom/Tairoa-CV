# 手臂視覺定位 + 氣動夾爪(筆電 + Arduino + L298N)相依套件
# 注意:opencv-contrib-python 與 opencv-python 不可同時安裝(會衝突)。
# 專案用到 cv2.aruco,一定要 contrib 版;視覺工具要開視窗,不要裝 -headless 版。

numpy>=1.24
opencv-contrib-python>=4.8

# Arduino USB 序列通訊(套件名 pyserial,程式裡 import serial)
pyserial>=3.5

# 只在 Windows 用「相機名稱」找編號;Linux 上會自動退回用相機編號
pygrabber; sys_platform == "win32"
